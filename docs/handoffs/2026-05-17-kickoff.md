# Kickoff handoff — 2026-05-17 (post-WS 0)

Companion to `2026-05-17.md` (the pre-kickoff state). This file = the post-kickoff state after WS 0 shipped.

## What happened this session

- Read pre-kickoff handoff + MVP bundle plan
- Grilled 3 locked decisions:
  1. **Merge now** — ship launch site to prod
  2. **Homepage HTML replaces /** — current launch v2 moves to `/launch` during DM transition
  3. **WS C is next** — skip WS A (brand drift), jump to admin portal port (accepts visual rework cost)
- Executed WS 0:
  - FF-merge `worktree-launch-site` → master (`b1600ea..292320c`)
  - Vercel auto-deploy READY (`dpl_EueWZVZNcNDoCQrAtarCCDS1W2X5`, prod, region iad1)
  - Smoke pass on `shippingcowmvp.vercel.app`: `/`, `/how-it-works`, `/pricing`, `/quote/submitted` → all 200; hero "Stop Getting Milked" + Calf/Cow/Bull tiers verified
  - Preserved two untracked docs (handoff + MVP plan) via second commit `21b495d`
  - Plan WS B section updated to lock the "replace /" decision (`B.0` move existing → `/launch`, `B.1` replace `/`, `B.11–B.13` updated)
  - Deleted remote branch `worktree-launch-site`
  - **Local worktree NOT yet removed** — see Cleanup below

## State

- **Master:** `21b495d` (pushed). Prod live: `https://shippingcowmvp.vercel.app/`
- **Worktree:** `.claude/worktrees/launch-site` still on disk, on local branch `worktree-launch-site`. Cleanup is Jay's call.
- **Open Vercel deploy:** the `21b495d` docs commit will trigger another auto-deploy (no app code changed; safe).

## Cleanup oneliner (from main repo, when convenient)

```bash
cd /Users/jayos/code/shippingcow-admin
git worktree remove .claude/worktrees/launch-site
git branch -d worktree-launch-site
```

## Blocked on Jay

1. Cal.com event `jay-shippingcow/intro-demo` still needs to exist (smoke notes the `/quote/submitted` page renders; the inner Cal embed still 404s until Jay creates the event in his dashboard)
2. CLAUDE.md updates pending approval (9 changes mentioned in prior handoff — still untouched; not blocking WS B/C)
3. DM batch rotation timing — once homepage replaces `/`, decide when to delete `/launch` (week 1 of WS B is fine)

## Next move

**WS C kick-off** — write detailed sub-plan for admin portal port (9 days, 8 sections). Subagent-driven-development discipline says: write the WS C sub-plan in a fresh session, then execute. Path: `docs/superpowers/plans/2026-05-18-ws-c-admin-portal.md` (or whatever date the next session opens).

WS B can run in parallel with WS C once started — both are unblocked.

## Open tasks (session-local, will reset)

```
#4 WS 0.6 — Worktree + branch cleanup, ExitWorktree (in_progress; remote branch deleted, local cleanup deferred to Jay)
#6 WS C kick-off — write sub-plan for admin portal port (pending; next session)
```
