# Session handoff — WS B shipped (2026-05-18)

## What happened

WS B (homepage HTML port) executed end-to-end via `superpowers:subagent-driven-development`. 8 tasks (T1–T8) each gated by spec + code-quality subagent reviews; 3 of the 8 required fix dispatches after first review (T4, T5, T6 each had NEEDS_CHANGES). One final branch-level reviewer caught a real bug the per-task reviewers missed (nav `currentPath` on `/launch`).

14 commits FF-merged to master → prod auto-deployed.

## State

- **Master:** `fe65ca0` (pushed; prod live)
- **Prod URL:** `https://shippingcowmvp.vercel.app/` — homepage HTML now serves at `/`. Launch v2 at `/launch`.
- **Smoke pass:** `/`, `/launch`, `/how-it-works`, `/pricing`, `/quote/submitted` all 200. Homepage hero ("Moo-ve Your Heavy Goods") + DIM + Shrinkage + final CTA verified. Launch v2 still shows "Stop Getting" + "Heavy Freight".
- **Worktrees:** all cleaned up (none leftover).
- **Tests:** 136 vitest (was 8 — +34 dim-calc, +80 shrink-calc, +14 fmt) + 6 Playwright e2e (was 1 — +5 homepage-flow).

## What shipped

- New `app/page.tsx` (~1300 lines, 9 sections): hero, pain, tools (DIM + Shrink + CTA bar), built-by-operators, cow-guaranteed, testimonials, services, final CTA.
- `app/launch/page.tsx` — launch v2 moved here during DM transition window.
- `lib/dim-calc.ts`, `lib/shrink-calc.ts`, `lib/fmt.ts` — pure math + format helpers, fully unit-tested.
- `app/_dim-calculator.tsx` + `app/_shrink-calculator.tsx` — `'use client'` leaves wired into the server page.
- `BRAND.muted` + `BRAND.sky` token additions (`lib/brand.ts` + `tailwind.config.ts`).
- 3 image assets (cow-pixel, warehouse-illustration, cow-logo) — see Asset debt below.
- PublicNav: "Launch" transition link added; CTA targets `#inquiry`.
- Metadata on both `/` and `/launch`.

## Accepted deviations (prototype-SoT rule)

- `borderRadius: 6` on final CTA buttons (deviates from CLAUDE.md "zero radius on interactive" — prototype-verbatim).
- 3 public images are byte-identical JPEGs with PNG extensions, 4000×4000 each (~377KB). The prototype HTML reused the same base64 data URI in 3 different slots. Faithful port. The hero cow, warehouse illustration, and final CTA logo will render the SAME image until real assets land.

## Asset debt (priority for next polish pass)

1. Replace `public/cow-pixel.png` with an actual pixel-art cow at ~64–256px (right now it's a 4000×4000 high-res JPEG; `imageRendering: pixelated` is semantically wrong for it).
2. Replace `public/warehouse-illustration.png` with a real warehouse illustration (currently same byte-identical image as cow-pixel).
3. Replace `public/cow-logo.png` with a real brand logo (currently same byte-identical image).
4. After 1–3, all three images should have HTML `width`/`height` attributes (T6 added them only on cow-logo).

Total image payload today: ~1.1MB for 3 images that should be small icons. Real fix here cuts page weight by 90%+.

## Other minor debt (not blocking)

- `_dim-calculator.tsx` has 3 in-JSX hex literals without `// verbatim` comments on the same line (T8 final review note; consistent values but inconsistent commenting style).
- `_dim-calculator.tsx`'s `sc*` S keys ("savings callout") collide visually with shrink-calculator's `sc-*` DOM ids — zero functional risk, mild reader confusion.
- E2E specs use `http://localhost:3001/...` instead of relative paths — codebase-wide pattern but `playwright.config.ts` defines `baseURL` that's currently unused.
- OG/Twitter metadata not added (`title.template` doesn't propagate to `og:title` in Next.js by default). Deferred SEO task.

## Subagent-driven-development scorecard

| Task | Round 1 verdict | Fix count | Final |
|---|---|---|---|
| T1 routing | APPROVED | 0 | APPROVED |
| T2 hero | APPROVED | 0 | APPROVED |
| T3 static sections | APPROVED | 0 | APPROVED |
| T4 DIM calc | NEEDS_CHANGES | 1 | APPROVED |
| T5 Shrink calc | NEEDS_CHANGES | 1 | APPROVED |
| T6 closing sections | NEEDS_CHANGES (spec) + NEEDS_CHANGES (quality) | 2 | APPROVED |
| T7 nav + metadata | APPROVED (minor inline fix) | 0 (1 inline edit) | APPROVED |
| T8 e2e tests | APPROVED | 0 | APPROVED |
| Final branch review | NEEDS_CHANGES (currentPath bug) | 1 inline | READY TO SHIP |

Per-task review caught ~10 issues that would have hit prod. Final branch review caught 1 real bug per-task reviews missed (cross-cutting). Subagent pattern paid off.

## Blocked on Jay

1. **Cal.com event `jay-shippingcow/intro-demo`** still needs to exist — `/quote/submitted` page renders but the Cal embed 404s until you create the event in your Cal.com dashboard.
2. **Pre-existing CLAUDE.md stale references** (9 changes pending approval from kickoff handoff) — not blocking anything.
3. **DM batch rotation timing** — once you're comfortable with the new `/` as the DM landing page, decide when to delete the `/launch` route + remove the "Launch" link from PublicNav.

## Next move

**WS C kick-off** still queued from kickoff. Write detailed sub-plan for admin portal port (9 days, 8 sections). Then execute via subagent-driven-development.

Optional parallel: asset-polish task (3 image replacements per the Asset debt list above) — can run in parallel with WS C since they don't touch admin code.
